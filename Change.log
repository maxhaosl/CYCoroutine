CYCoroutine V1.0.0 features:
1. Base C++ 20 coroutine implementation
2. The interface is simple and easy to use and supports cross-platform.
3. Thread Pool.
4. Timer.
5. Ranges.
6. concept constraint.
7. coroutine lock.

Included plugins:

2025-11-24：
1. `Build/build_mac.sh` 在完成 arm64 与 x86_64 构建后自动调用 `lipo` 生成 `Bin/macOS/universal/<配置>` 下的通用静态、动态库。
2. `Build/build_ios.sh` 在至少检测到两个 iOS 架构产物时创建通用库，方便直接提供给 CYLogger 及其他上层项目。
3. README 更新了 Apple 平台通用库产物及使用方式的说明。
4. README 记录了 Linux 平台默认要求 `clang-17`/`clang++-17`，并提供 `CYLOGGER_CC` / `CYLOGGER_CXX` 覆盖示例，确保与 CYLogger 依赖构建使用相同工具链。

2025-11-24（Windows 静态库限定）：
1. `Build/build_windows.bat` 修复了 `%SOURCE_DIR%` 尾随反斜杠导致的 CMake 解析错误，并统一 CRLF 行结尾避免 label 查找失败。
2. Windows 构建脚本现在只接受 `Static` 库类型，会在入口处拒绝 `Shared` 选项；静态构建仍会编译 `CYCoroutineExample`，而共享库相关目标已停用。
3. `Build/build_windows_all.bat` 默认 `LIB_LIST=Static`，并在矩阵循环中显式跳过 Shared 组合，保持 `Bin/Windows/<arch>/<CRT>/<config>` 目录下只有静态 `CYCoroutine.lib`。

2025-4-1：
1. Fixed the compilation problem of VS engineering different platforms.
